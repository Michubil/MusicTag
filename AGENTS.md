# Music Tag 开发约定

- 先检查 Git，保留已有修改；只做本轮授权范围内的工作。
- 文件按当前职责命名，单一主要类型与文件同名，相关功能集合使用明确的职责名；职责变化时同步检查文件名、主要声明、测试名及引用，不保留误导性的旧业务或版本名称。
- 通常一起阅读、一起修改的代码优先放在同一文件；不因代码只有几十行或新增一个类型就拆文件，职责独立且需要单独定位时再分开。
- 普通函数使用 lowerCamelCase，动作与对象应反映实际行为；区分读取、转换、取消任务、清空状态和删除数据，布尔判断使用 is/has/can/should 等明确语义。Compose UI 组件保留 PascalCase，框架回调及 override 遵循原契约。
- 版本和依赖以 Gradle 为准；保留包名、设置、签名及许可，不顺带升级依赖。依赖升级默认选 Stable/LTS，已授权例外除外。
- Kotlin / Compose，Android 15+、竖屏 ARM64；不再要求或专项验证 16 KB 内存页支持。GUI 只用 `:core:designsystem`，不另写视觉常量或跟进上游样式。
- Room 仅保存可重建的目录、时长和浏览预览缓存，不保存匹配历史或任务状态，不恢复可见返回按钮。MP3 支持 ID3v2.3 / ID3v2.4，写入版本由设置选择。
- 使用 PowerShell 7.6+、无 Profile；支持时设 `login=false`。Gradle 用 `gradlew.ps1`，Android 工具直接用 JAR/EXE；不改机器配置或原生参数传递默认值。
- 没有用户明确要求时，不运行测试或构建打包。获准后用现有脚本验证；APK 放 `apk/`，不提交、不打包开发文件。交付如实区分自动化与真机验证。
- 文档只写当前必要约定，不追加会话记录、测试流水账或重复版本信息。

按任务读取：[项目说明](README.md)、[GUI](docs/design-system.md)、[音频与 SAF](docs/saf-storage.md)、[构建](scripts/README.md)。实现细节查对应源码和测试，不要求通读全部文档。
